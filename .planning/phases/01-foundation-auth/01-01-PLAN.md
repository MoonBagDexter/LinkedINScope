---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tailwind.config.js
  - postcss.config.js
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/contexts/WalletProvider.tsx
  - src/components/Header.tsx
  - src/utils/formatting.ts
  - .env.example
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "User can connect Phantom wallet via UI button"
    - "User session persists across browser refresh without re-authentication"
    - "Connected wallet address displays in header (truncated format)"
    - "User can disconnect wallet"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "@solana/wallet-adapter-react"
    - path: "src/contexts/WalletProvider.tsx"
      provides: "Wallet context wrapper with autoConnect"
      exports: ["WalletProviderWrapper"]
      min_lines: 20
    - path: "src/components/Header.tsx"
      provides: "Wallet connection UI"
      min_lines: 15
    - path: "src/App.tsx"
      provides: "Application shell with wallet context"
      min_lines: 10
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React render"
      pattern: "createRoot.*App"
    - from: "src/App.tsx"
      to: "src/contexts/WalletProvider.tsx"
      via: "Context wrapper"
      pattern: "WalletProviderWrapper"
    - from: "src/components/Header.tsx"
      to: "@solana/wallet-adapter-react"
      via: "useWallet hook"
      pattern: "useWallet"
---

<objective>
Initialize the project with Vite, React, TypeScript, and Tailwind, then integrate Phantom wallet authentication with session persistence.

Purpose: Establishes the foundation for a crypto-native job board where users authenticate via Solana wallet. Session persistence (autoConnect) is critical for UX - users should not need to reconnect on every page load.

Output: A working React app where users can connect/disconnect Phantom wallet, see their truncated address, and maintain session across refresh.
</objective>

<execution_context>
@C:\Users\thedi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\thedi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React TypeScript Project with Tailwind</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tailwind.config.js
    postcss.config.js
    src/main.tsx
    src/App.tsx
    src/index.css
    .env.example
    .gitignore
  </files>
  <action>
1. Create Vite project with React + TypeScript template:
   ```bash
   npm create vite@latest . -- --template react-ts
   ```

2. Install core dependencies:
   ```bash
   npm install @solana/wallet-adapter-react @solana/wallet-adapter-react-ui @solana/wallet-adapter-wallets @solana/web3.js
   npm install @tanstack/react-query
   npm install axios
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

3. Configure tailwind.config.js for degen aesthetic:
   - Content paths: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
   - Theme extends with custom colors: purple accent, high contrast, crypto-native palette
   - Dark mode: 'class'

4. Configure src/index.css with Tailwind directives and base dark theme:
   ```css
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

   body {
     @apply bg-black text-white;
   }
   ```

5. Create .env.example with required env vars (VITE_JSEARCH_API_KEY placeholder)

6. Update .gitignore to include .env.local, node_modules, dist

7. Create minimal src/App.tsx placeholder that renders "LinkedInScope" heading

8. Verify: `npm run dev` starts without errors
  </action>
  <verify>
    - `npm run dev` starts development server successfully
    - Browser shows placeholder App content at localhost:5173
    - No TypeScript errors in console
    - Tailwind classes apply (test with bg-purple-500 class)
  </verify>
  <done>
    Vite + React + TypeScript + Tailwind project initialized and running locally with dark theme base styles applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Solana Wallet with Session Persistence</name>
  <files>
    src/contexts/WalletProvider.tsx
    src/components/Header.tsx
    src/utils/formatting.ts
    src/App.tsx
    src/main.tsx
  </files>
  <action>
1. Create src/utils/formatting.ts:
   - Export `truncateAddress(address: string, chars = 4): string` function
   - Returns first 4 and last 4 characters with "..." in between

2. Create src/contexts/WalletProvider.tsx:
   - Import ConnectionProvider, WalletProvider from @solana/wallet-adapter-react
   - Import WalletModalProvider from @solana/wallet-adapter-react-ui
   - Import PhantomWalletAdapter from @solana/wallet-adapter-wallets
   - Import clusterApiUrl from @solana/web3.js
   - Import wallet adapter CSS: '@solana/wallet-adapter-react-ui/styles.css'
   - Configure:
     * endpoint = clusterApiUrl('mainnet-beta')
     * wallets = [new PhantomWalletAdapter()]
     * autoConnect = true (critical for session persistence)
   - Export WalletProviderWrapper component that nests:
     ConnectionProvider -> WalletProvider -> WalletModalProvider -> children

3. Create src/components/Header.tsx:
   - Import useWallet from @solana/wallet-adapter-react
   - Import WalletMultiButton from @solana/wallet-adapter-react-ui
   - Display truncated wallet address when connected
   - Style with degen aesthetic: dark bg, purple/pink accents, bold font
   - Include app title "LinkedInScope" with tagline "Solana nuked, time to flip burgers"

4. Update src/App.tsx:
   - Import WalletProviderWrapper and wrap content
   - Import QueryClient, QueryClientProvider from @tanstack/react-query
   - Configure QueryClient with aggressive caching:
     * staleTime: 24 hours (1000 * 60 * 60 * 24)
     * gcTime: 48 hours
     * refetchOnWindowFocus: false
     * refetchOnMount: false
     * retry: 1
   - Render Header component
   - Add placeholder for job list area with "Jobs will appear here" text

5. Test wallet connection:
   - Click connect button (opens Phantom if installed)
   - After connecting, address should show truncated in header
   - Refresh browser - should auto-reconnect (session persistence)
   - Click disconnect - should clear connection
  </action>
  <verify>
    - Wallet connect button renders and opens modal on click
    - After connecting, truncated address displays (e.g., "HN7c...YWrH")
    - Browser refresh maintains wallet connection (autoConnect works)
    - Disconnect button clears the session
    - No console errors about missing context providers
  </verify>
  <done>
    Phantom wallet authentication working with autoConnect session persistence. User can connect, see their address, and remain connected across page refresh.
  </done>
</task>

</tasks>

<verification>
After completing both tasks, verify the full auth flow:

1. Start dev server: `npm run dev`
2. Open browser to localhost:5173
3. Verify dark theme and degen styling visible
4. Click wallet connect button
5. Connect Phantom wallet (must have Phantom browser extension)
6. Verify address shows truncated in header
7. Refresh the page (Cmd/Ctrl+R)
8. Verify wallet auto-reconnects without user action (AUTH-02)
9. Click disconnect
10. Verify address no longer shows
</verification>

<success_criteria>
- Project runs with `npm run dev` (no errors)
- Wallet connect button visible and functional
- Phantom wallet connects successfully
- Address displays in truncated format
- Session persists across browser refresh (autoConnect)
- Wallet can be disconnected
- Dark degen aesthetic applied (black bg, purple accents)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
